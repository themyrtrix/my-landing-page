"use client";
import { Home, ShoppingBag, Ticket, Heart, Settings, X } from "lucide-react";
import { cn } from "@/lib/utils";
const items = [{ label: "Home", icon: Home }, { label: "Cart", icon: ShoppingBag }, { label: "Coupons", icon: Ticket }, { label: "Favorite Items", icon: Heart }, { label: "Settings", icon: Settings }];
export function Sidebar({ mobile = false, onClose }: { mobile?: boolean; onClose?: () => void }) {
  return <aside className={cn("flex flex-col", mobile ? "h-full p-5" : "sticky top-24 hidden h-[calc(100vh-7rem)] w-56 shrink-0 md:flex")}><div className="mb-8 flex items-center justify-between"><span className="text-xs font-semibold uppercase tracking-[.18em] text-muted">Workspace</span>{mobile && <button onClick={onClose} aria-label="Close menu"><X size={18} /></button>}</div><nav className="space-y-1">{items.map(({ label, icon: Icon }, i) => <a key={label} href={i === 0 ? "#home" : undefined} className={cn("flex items-center gap-3 rounded-2xl px-4 py-3 text-sm transition", i === 0 ? "bg-accent/10 font-medium text-accent" : "text-muted hover:bg-black/[.04] hover:text-ink dark:hover:bg-white/[.08]")}><Icon size={18} strokeWidth={i === 0 ? 2.2 : 1.8} />{label}{i === 1 && <span className="ml-auto grid size-5 place-items-center rounded-full bg-black/[.06] text-[10px] dark:bg-white/10">2</span>}</a>)}</nav><div className="mt-auto border-t hairline pt-5 text-[11px] leading-5 text-muted">© 2024 Lumina<br/><a href="#privacy" className="hover:text-ink">Privacy Policy</a> · <a href="#terms" className="hover:text-ink">Terms</a></div></aside>;
}
